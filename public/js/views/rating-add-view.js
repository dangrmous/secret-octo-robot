var $ = require('jQuery'), Backbone = require('backbone');

Backbone.$ = $;

var ratingAddTemplate = require('../../templates/rating-add-view.hbs');

var RatingAddView = Backbone.View.extend(
    {

    }
)

module.exports = RatingAddView;